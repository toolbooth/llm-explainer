# Classroom Edition — phase 4 review sheet (REVIEW-CLASSROOM-4) — resumed and C shipped 2026-09-15

Phase 4 was planned as four commits (A–D). **A and B landed 2026-08-23; C landed 2026-09-15** (resumed from `git stash@{0}` after the site went live at insidethemachine.org and essay #5 shipped in between; the stash entry is kept for the record). **D remains.** Verification at the C close-out: 245 tests green across 32 files, `tsc --noEmit` clean, `vite build` clean, all **ten** HASHES.md rows reproduced by `npm run check:hashes` (the two essay-5 rows joined the baseline between B and C; the essays stayed byte-identical through every C commit), `npm run audit:a11y` re-run over the grown 29-target list — 48 page states, **zero axe violations at every level**.

## Commit A — `d758ebd` WCAG 2.1 AA pass (done)

- **Audit**: `npm run audit:a11y` (scripts/audit-a11y.mjs; axe-core 4.13.0 via playwright-core + local Chrome; src/classroom/audit.ts = target list, report shape, gate; test/a11y-audit.test.ts). 19 routes × EN/zh, lesson pages also after driving every hint/chip/roll/step/slider/table = 38 page states.
- **Before**: 6 critical + 18 serious (128 nodes): unnamed Gamble/Loop text fields and sliders (`label`), `.act-num` ink 2.3:1 and 75 %-opacity token ids (`color-contrast`), scrolling tables / embed `<pre>` unreachable by keyboard (`scrollable-region-focusable`). **After**: 0 critical / 0 serious / 0 moderate / 0 minor at 1280 px and at 375 px; axe best-practice rules (landmarks, heading order) also 0.
- **Fixes** (all behind an optional `a11y` prop → essays' DOM byte-identical): Chopper piece list + live count; Gamble named field/slider (aria-valuetext), bar list, live "picked" line; TheLoop named field/slider, each written word a `<button>` (Enter opens alternatives, Escape closes), sr-only status line, "Show the step log" table, reduced-motion run lands at once (30 words < 120 ms vs 4.3 s paced, no cursor); X-ray letters row as one phrase, piece list; Hundred Rolls localized slider name. Pages: skip link (focus, not hash), `<main>`/banner/contentinfo, 3 px focus ring, focusable named TableWrap + embed `<pre>`, `th scope`, `lang` on glossary columns, per-block `lang` heuristic in Markdown.tsx (blockLang). Light-theme contrast verified computed over every text node of M1/M2/crosswalk: min 4.7:1 (X-ray hit letter); only disabled "Reset rolls" (2.4:1, exempt) below 4.5.
- **Keyboard walk** with real key events (Playwright): skip link → `#cl-main` with hash unchanged; Loop token button opens/closes popover (aria-expanded true/false, 8 rows); slider ArrowRight "T = 1.00" → "T = 1.10"; Roll by Enter updates live region; Hundred Rolls by Enter → 100 rolls, table 10 rows; zh names render ("第 2 块:“raw”,编号 1831"). **Not done**: any real screen reader (ChromeVox/VoiceOver/NVDA) — the statement says so, first known-gap row.
- Accessibility statement (EN + zh) rewritten to the audited truth; guides' notes + index blurb updated.

## Commit B — `21e6fc7` service worker (done)

src/classroom/sw/{strategy,classroom-sw,register,vite-plugin}.ts → `dist/classroom-sw.js` (1.9 KB, no framework): versioned precache (content hash of 8 files, 10.9 MB: shell, 3 chunks, weights, tokenizer), network-first navigations (never stale HTML), cache-first precached paths (pathname match, ignoreVary), everything else bypassed, nothing cached at runtime; registered only from `#/classroom…` in production. **Offline proof** (headless Chrome on `vite preview`, real weights): worker active 2.0 s after model load; context offline; reload → M2 renders, Gamble 10 bars, Hundred Rolls rolls, Loop steps, 0 failed requests, un-precached fetch fails; offline nav to M1 zh + tech check OK; essays/flagship/dev register nothing. Tech check (EN+zh) new "Offline after the first visit" section; model card/privacy say built; bundle re-measured 1.29 MB disk / ~412 KB gz (total 10.90 MB disk, ~8.5 MB wire). +10 tests (test/classroom-sw.test.ts).

## Commit C — embed kit + evidence surface (SHIPPED 2026-09-15, as four commits `8dbeb4a` → `fbed3bb`)

- **`8dbeb4a` plumbing.** The stash's placeholder machinery landed carrying the now-real values: `CLASSROOM_ORIGIN = "https://insidethemachine.org"` (the classroom edition lives on the flagship's own domain under `#/classroom`, not a `classroom.` subdomain; pinned equal to `CITE_URL`) and `CLASSROOM_REPO_URL = "https://github.com/toolbooth/llm-explainer"` (pinned against CITATION.cff); `hasOrigin` / `embedOrigin` / `hasRepo` kept as the guard if either is ever unset (tests pin the placeholder path too). Route kinds `embed` (`#/classroom/embed`, plus `#/classroom/embed/<chopper|gamble|hundred-rolls>`) and `taught`; `src/classroom/embed.ts` (cleanUrl with `?lang=`, previewUrl carrying `mockModel` through, escapeHtml, embedSnippet = one fixed-height iframe + fallback link, 13 whole-page targets, 3 widget surfaces with fixed heights 620/840/900 px sized against measured render heights).
- **`767c6a4` embed kit.** `#/classroom/embed`: per-widget cards (blurb, clean URL, snippet in a focusable `<pre>`, copy button that falls back to revealing the snippet when the clipboard is denied, live preview iframe — same origin, deterministic under `?mockModel=1`) and the whole-page table (every lesson page, step 1–3, guide, printable, M2 slides × URL/height/snippet). `#/classroom/embed/<widget>` renders the lesson page's own widget component with the same strings, the classroom temperature cap and the a11y chrome — the step chip swapped for the module label — plus a **visible attribution footer linking back to insidethemachine.org** (doubles as the way out of the frame). Both guides' §14 now print the shared snippet at the real origin; `classroom.YOUR-DOMAIN` is gone from every printed snippet and guide; the `ORIGIN_PLACEHOLDER` constant stays in config.ts as the documented fallback guard. EN + 中文 with deep key parity.
- **`77c91a2` evidence surface.** `#/classroom/taught` (PRODUCT.md §8): the public adopter list renders from the checked-in `src/classroom/adopters.ts` (fields mirror the letter kit's §A report — name or self-chosen anonymous label, institution optional, course, term, modules, note, source, recorded consent; **the schema has no field for student-level data, on purpose**, and `validateAdopters` + test/adopters.test.ts enforce consent, known modules, real dates, and that a discussion source lives on the project's own board). Ships empty with a designed empty state that is itself the invitation; §8.1 rules; three channels (Discussion template at `.github/DISCUSSION_TEMPLATE/i-taught-with-this.yml` — **the category still needs one-time creation in the repo's Discussions settings** — prefilled `mailto:`, letter kit); dual-use consent in plain words, EN + 中文. Index links both tool pages. Audit targets grew to 29 (embed kit, 3 widget surfaces, taught); the re-run audit is the zero-violation record above.
- **`fbed3bb` REVIEW-3 fixes.** (a) **HS-SOC-HU-44** resolved per PRODUCT.md **§4.3 item 6** (not the earlier plan's "drop the row per §4.2" — §4.3 draws every module's block-extension debate from this standard, so the guide's claim was right and needed scoping, not deletion): the crosswalk's M2 row (EN+中文) now names it scoped "block-extension debate only", the full-module mapping stays M3, the M2 guide's row carries the same scope, and the re-verification log records the resolution dated 2026-09-15. (b) **Bundle budget** re-baselined in PRODUCT.md §6.1 (r3, dated rationale + revision-log row): measured table from `npm run build` 2026-09-15 — app+shell 0.82 MB, transformers.js chunk 0.56 MB, tokenizer 2.11 MB, weights 7.50 MB = **10.99 MB on disk, ~8.5 MB gzipped on the wire**; budget restated ≤ 11 MB disk; 30-client burst ~330 MB; the tokenizer-rewrite alternative declined (token-exact-verified engine vs ~0.16 MB of wire); weights+tokenizer < 10 MB keeps its test; tech check EN+中文 cross-referenced and the old 1.29/10.90 numbers test-rejected.
- **Deviations from the C plan, with reasons.** (1) OG meta + build-time static route stubs (`og.ts` + Vite plugin) were **not built** — deferred to D: Cloudflare Pages direct upload serves the SPA shell for every path, the hash router owns the URL space, and shipping OG stubs deserves its own verified commit rather than a rider. (2) The policy-page EO 14277 Sec. 6 / priorities (a)(ii)/(a)(ix)/(a)(x) revision (REVIEW-3 open question 3) was **not made** — the task brief scoped the REVIEW-3 fixes to the crosswalk and the budget; the policy rewrite is a sourced-writing task and stays open. (3) The front-matter `[… — TBD]` placeholders: the letter kit already named the real repo, and the remaining privacy-page `[date — TBD, planned Nov 2026]` (Common Sense evaluation request) is genuinely still unset — left as is.
- **Claims-care.** Every embed loads only this origin (the locality test and the embed snippet's single URL keep it true); "deterministic under `?mockModel=1`" is the mock engine's property, relied on by the audit and previews; the taught page says "this site went live in September 2026" (true of the site) and claims no adopters — the list is empty and says so; the attribution line's "nothing typed here leaves the device" is the same claim the privacy page makes of the engine. Nothing on any new page presents as a chat product (§6.2 test extended to the new tables).

## Commit D — close-out docs and launch chores (REMAINS; updated 2026-09-15)

- **Ops note (added 2026-09-16):** the site deploys by manual direct upload (`npm run build` + `wrangler pages deploy dist --project-name inside-the-machine`); a push does NOT deploy. Phase C reached the live domain on 2026-09-16. Every classroom-visible change needs that command.

Still owed by phase D, in rough order of leverage:

1. `src/classroom/README.md` — architecture, routes (now incl. embed/taught), config constants, adopters data-file workflow, build:pdf / audit:a11y / check:hashes, service worker, how to add M3.
2. **One-time GitHub chores the code now points at**: create the "I taught with this" Discussions category (slug `i-taught-with-this`) so the checked-in template renders; confirm Discussions are enabled on the public repo.
3. **OG cards** (moved out of C): og.ts + build-time static stubs per classroom route, or a decision to skip them with reasons on the record.
4. **Policy-page revision against PRODUCT r2** (REVIEW-3 open question 3): resolve the EO 14277 Sec. 6 caveat, add priorities (a)(ii)/(a)(ix)/(a)(x) with the "how this aligns" sentence, EN + 中文, list-length parity.
5. Re-render the PDF set (`npm run build:pdf`) after the guide §14 changes and spot-check the two guides' embed pages.
6. The §10.1 "done means" gaps, each author/hardware-dependent: real-teacher pilot; 4 GB Chromebook / iPad / 30-client tests (the 330 MB burst arithmetic is now the r3 number); filter-category submissions (the domain is live, so the 60-day categorization clock can start); **weights-license closure before distribution pushes** (the site is live; the model card still says "research release, redistributed unchanged with attribution; license request pending" — that answer, or the retrain fallback, is still the §10.1 blocker for *promoting* the classroom edition); Hour of AI catalog + Common Sense Privacy Program submissions (§7.1 — the September window is now).
7. The bundle-budget decision is **closed** (C, `fbed3bb`); strike it from this list's predecessor.
